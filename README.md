# CallBrief (Hasamex technical case)

The working application lives in [`web/`](./web).

```bash
cd web
npm install
cp .env.example .env.local   # optional GROQ_API_KEY
npm run dev
```

See [`web/README.md`](./web/README.md) for architecture, features, and demo notes.
